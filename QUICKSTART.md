# 🚀 快速开始指南

## 安装和运行

### 1. 安装依赖
```bash
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```

然后在浏览器中打开 `http://localhost:5173`

## 🎨 个性化定制

### 修改个人信息

#### Hero 部分 (`src/components/Hero.jsx`)
- **第 98 行**: 修改你的名字
- **第 150-170 行**: 更新社交媒体链接

#### About 部分 (`src/components/About.jsx`)
- **第 85-95 行**: 更新个人简介
- **第 100-117 行**: 修改教育背景、研究方向和技能

#### Research 部分 (`src/components/Research.jsx`)
- **第 7-36 行**: 添加你的研究项目

#### Publications 部分 (`src/components/Publications.jsx`)
- **第 5-44 行**: 添加你的论文发表

#### Contact 部分 (`src/components/Contact.jsx`)
- **第 6-29 行**: 更新联系方式

### Timeline 时间线 (`src/components/Timeline.jsx`)
- **第 6-40 行**: 添加你的学术里程碑

## 🎨 交互功能

### 1. 涂鸦板
- 点击右下角的铅笔图标打开涂鸦板
- 选择颜色和画笔大小
- 可以下载你的作品

### 2. 鼠标轨迹
- 移动鼠标查看彩色轨迹效果
- 在页脚可以切换开关

### 3. 粒子背景
- 粒子会随鼠标移动而互动
- 自动连接附近的粒子

### 4. 项目过滤
- 在 Research 部分点击标签过滤项目

### 5. 技能可视化
- 在 About 部分悬停在技能上查看进度条动画

## 🎨 颜色主题

主要颜色定义在 `tailwind.config.js`:
- **paper**: `#FFFEF9` - 纸张背景色
- **ink**: `#2D3748` - 主要文字颜色
- **sketch**: `#4A5568` - 次要文字颜色
- **粉色系**: `#FF6B9D`, `#FF85A6` - 杜鹃花主题色
- **紫色**: `#6B5FFF` - 强调色

## 🌸 杜鹃花元素

杜鹃花组件 (`src/components/Azalea.jsx`) 可以自定义:
- `size`: 花朵大小
- `color`: 花朵颜色
- `animate`: 是否显示绘制动画

使用示例:
```jsx
<Azalea size={80} color="#FF6B9D" animate={true} />
```

## 📦 构建和部署

### 构建生产版本
```bash
npm run build
```

### 部署到 GitHub Pages

1. 在 `vite.config.js` 中更新 `base` 为你的仓库名:
```javascript
base: '/your-repo-name/',
```

2. 运行部署命令:
```bash
npm run deploy
```

3. 在 GitHub 仓库设置中启用 GitHub Pages (选择 `gh-pages` 分支)

## 🐛 常见问题

### 样式没有加载
确保 Tailwind CSS 正确配置，运行:
```bash
npm install -D tailwindcss postcss autoprefixer
```

### 粒子效果卡顿
可以在 `InteractiveBackground.jsx` 中减少粒子数量 (第 24 行)

### 动画不流畅
检查浏览器是否支持 CSS 动画，或在 `index.css` 中调整动画时长

## 💡 提示

- 所有动画都在 `src/index.css` 中定义
- 使用 `sketch-card` 类创建手绘风格卡片
- 使用 `font-handwriting` 类应用手写字体
- 杜鹃花装饰可以在任何组件中使用

## 📝 文件结构

```
src/
├── components/
│   ├── About.jsx              # 关于我
│   ├── Azalea.jsx            # 杜鹃花组件
│   ├── Contact.jsx           # 联系方式
│   ├── CursorTrail.jsx       # 鼠标轨迹
│   ├── Hero.jsx              # 首页
│   ├── InteractiveBackground.jsx  # 交互背景
│   ├── Navigation.jsx        # 导航栏
│   ├── Publications.jsx      # 论文发表
│   ├── Research.jsx          # 研究项目
│   ├── SketchPad.jsx         # 涂鸦板
│   ├── SkillsVisualization.jsx   # 技能可视化
│   └── Timeline.jsx          # 时间线
├── App.jsx                   # 主应用
├── index.css                 # 全局样式
└── main.jsx                  # 入口文件
```

祝你使用愉快！🌸✨
